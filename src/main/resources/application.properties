spring.application.name=Instagram

# ===============================
# MYSQL DATABASE CONFIG
# Database Connection Settings
spring.datasource.url=jdbc:mysql://localhost:3306/InstagramClone?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Rathor@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
# 'update' will automatically create/update your tables based on your @Entity classes
spring.jpa.hibernate.ddl-auto=update

# Show SQL in console (very helpful for debugging your Follow logic)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Database Dialect
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect